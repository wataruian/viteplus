name: 'PR Pipeline'

on:
  pull_request:
    branches: [main, ci]

concurrency:
  group: pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  pull-request:
    permissions:
      contents: read
      actions: write
    uses: ./.github/workflows/check.yml
    secrets: inherit
