name: 'Trigger'

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment'
        required: true
        type: choice
        options:
          - pr
          - develop
          - staging
          - production
      workspace:
        description: 'Workspace'
        required: true
        type: choice
        default: all
        options:
          - all
          - design-system
          - frontend
          - backend
      version:
        description: 'Git tag/ref to deploy (required for staging/production, ignored for pr)'
        required: false
        type: string

concurrency:
  group: trigger-${{ inputs.environment == 'pr' && format('pr-{0}', github.ref) || github.run_id }}
  cancel-in-progress: false

jobs:
  guard:
    name: Guard
    runs-on: [ubuntu-latest]
    timeout-minutes: 2
    steps:
      - name: Require version for staging/production
        if: inputs.environment == 'staging' || inputs.environment == 'production'
        shell: bash
        env:
          VERSION: ${{ inputs.version }}
        run: |
          if [[ -z "${VERSION}" ]]; then
            echo "::error::version is required when targeting staging or production"
            exit 1
          fi

      - name: Warn that version is ignored for pr
        if: inputs.environment == 'pr' && inputs.version != ''
        shell: bash
        run: |
          echo "::warning::version is ignored when targeting pr; verification always runs against the dispatched branch's tip"

  verify:
    name: 'Verify (branch: ${{ github.ref_name }})'
    needs: guard
    if: inputs.environment == 'pr' && github.ref != 'refs/heads/main'
    permissions:
      contents: read
      actions: read
      pull-requests: write
    uses: ./.github/workflows/verify.yml
    secrets: inherit
    with:
      workspace: ${{ inputs.workspace != 'all' && inputs.workspace || '' }}

  deploy:
    name: 'Deploy (environment: ${{ inputs.environment }})'
    needs: guard
    if: inputs.environment != 'pr' && github.ref == 'refs/heads/main'
    concurrency:
      group: deploy-${{ inputs.environment }}
      cancel-in-progress: ${{ inputs.environment != 'production' }}
    permissions:
      contents: read
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
    with:
      environment: ${{ inputs.environment }}
      workspace: ${{ inputs.workspace != 'all' && inputs.workspace || '' }}
      ref: ${{ inputs.version }}
