import type { IconsOptions } from 'unocss/preset-icons';

/**
 * Icons Tokens
 *
 * Defines configuration for UnoCSS icons preset.
 */

const iconsOptions: IconsOptions = {
  scale: 1.2,
  warn: true,
};

export { iconsOptions };
