{
  "name": "@lightproject/monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean:ai": "rimraf -g \".ai-data/artifacts/**/*\" \".ai-data/artifacts/!(*.gitkeep)\"",
    "clean:build": "vp cache clean && rimraf -g .vite-hooks/_ **/__unconfig_vite.config.ts packages/**/tmp/** apps/**/tmp/** **/node_modules/** **/dist/** **/coverage/** **/tsconfig.tsbuildinfo vite.config.d.ts vite.config.d.ts.map",
    "clean:tools": "rimraf -g **/tmp/**",
    "debug": "vp cache clean && vp run ready",
    "init": "vp install && vp run ready",
    "prepare": "vp config",
    "ready": "vp run -r check && vp run -r format && vp run -r lint && vp run -r type-check && vp run -r build && vp run -r test",
    "serve:dev": "vp run -r --parallel dev",
    "serve:prod": "vp run -r --parallel start",
    "setup": "./install.sh"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "bumpp": "catalog:",
    "madge": "catalog:",
    "rimraf": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "onFail": "download",
      "version": "11.10.0"
    }
  },
  "engines": {
    "node": "24.14.1"
  }
}
